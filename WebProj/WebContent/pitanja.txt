1. Da li se u metodama paralelno izvrsavaju stvari? Zato sto sam napravio recimo axios pa pozivao neku metodu koja vraca ulogu koju upisujem
u atribut i onda izvan tog axios-a(izvan njegovog then) printujem to sto je upisao na klik na neko dugme i ne upise se. Tek posle sledecg kliika
se upise. A onda sam stavio untar tog axiosa i sve je radilo.
Primer:
axios
	.post('rest/webproj/checkRole', {"email": this.User.username, "password": this.User.password})
	.then(response => {
		this.role = response.data;
		
		toast(this.role);
		if(this.role == "korisnik"){
			router.push({path: "/korisnik"});
		}
	});
	Ovo gore radi
	
axios
	.post('rest/webproj/checkRole', {"email": this.User.username, "password": this.User.password})
	.then(response => {
		this.role = response.data;
	});
	toast(this.role);
	if(this.role == "korisnik"){
		router.push({path: "/korisnik"});
	}
	Ovo gore radi tek na drugi klik. Kao da na prvi samo upise a jos pre toga se pozove ovo toast.
	
2. Da li je uredu da pravim za svaku ulogu posebnu kopmonentu(kor,admin,superadmin), odnosno da prilikom logovanja korisnika proveravam
u posebnoj metodi da li koja je njegova uloga, i da na osnovu toga pozivam odredjenu komponentu(templejt)? 
Primer: login.js, 'login' metoda.

3. 